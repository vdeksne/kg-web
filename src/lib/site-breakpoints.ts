/** Home hero + burger chrome: “phone” layout up to this width (inclusive). */
export const HOME_COMPACT_MAX_PX = 430;

/** First width that uses the wide-home layer (desktop raster, inline lang, etc.). */
export const HOME_WIDE_MIN_PX = HOME_COMPACT_MAX_PX + 1;
